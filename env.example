# .env.example
# Copie este arquivo para .env e preencha com seus valores

# =====================================
# CONFIGURAÇÕES DO SERVIDOR
# =====================================

# Porta do servidor (opcional, padrão: 3000)
PORT=3000

# Ambiente (development, production)
NODE_ENV=development

# URL base da aplicação (para webhooks e links de pagamento)
# IMPORTANTE: Em produção, use a URL real do Railway
APP_URL=https://mercadopago-link-generator-production.up.railway.app

# =====================================
# SEGURANÇA E AUTENTICAÇÃO
# =====================================

# Secret para JWT (OBRIGATÓRIO - use uma string aleatória forte)
# Para gerar um secret seguro, use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-secret-key-here-mudar-em-producao

# Chave de criptografia para tokens do Mercado Pago (OBRIGATÓRIO - 32 bytes em hex)
# Para gerar uma chave segura, use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANTE: Nunca mude esta chave após ter usuários cadastrados!
ENCRYPTION_KEY=coloque-aqui-64-caracteres-hexadecimais-gerados-com-openssl

# =====================================
# CONFIGURAÇÕES DO MERCADO PAGO
# =====================================

# Modo de teste do Mercado Pago (true = sandbox, false = produção)
MP_TEST_MODE=true

# Webhook Secret do Mercado Pago (opcional, para validar notificações)
# Configure este valor no painel do Mercado Pago
MP_WEBHOOK_SECRET=

# Nota: As credenciais do Mercado Pago (access_token e public_key)
# são configuradas por cada usuário individualmente no sistema

# =====================================
# BANCO DE DADOS
# =====================================

# Configurações do banco de dados (SQLite)
# O arquivo database.db será criado automaticamente
DATABASE_PATH=./database.db

# =====================================
# CORS E SEGURANÇA
# =====================================

# Origins permitidas para CORS (separadas por vírgula)
# Use * para permitir todas (não recomendado em produção)
CORS_ORIGIN=*

# =====================================
# LOGS E DEBUG
# =====================================

# Nível de log (debug, info, warn, error)
LOG_LEVEL=debug

# =====================================
# RATE LIMITING (opcional)
# =====================================

# Janela de tempo em ms (15 minutos)
RATE_LIMIT_WINDOW_MS=900000

# Máximo de requisições por janela
RATE_LIMIT_MAX_REQUESTS=100

# =====================================
# EMAIL (opcional, para notificações futuras)
# =====================================

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@example.com
